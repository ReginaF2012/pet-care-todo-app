<div class="container">
  <% if @todos.empty?%>
  <p>
    Looks like you don't have anything to do. <br>
    <a href="/todo-list-items/new">Add something to your list?</a>
  </p>
  <% else %>
    <h1>Your To Do List:<h1>
      <% @todos.each do |todo| %>
        <h3><%= todo.name %></h3>
        <h4><%= todo.complete == false ? "Not done!" : "Done!" %></h4>>
        <a href=<%= "/todo-list-items/#{todo.id}" %>>more info</a><br><br>
      <% end %>
    <p>
      <a href="/todo-list-items/new">Make a new item for your To Do List</a><br><br>
      <a href="/">Home</a>
    </p>
  <% end%>
</div>